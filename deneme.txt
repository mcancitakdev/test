Task ExpireCompletedAsync(
    DateTime now,
    CancellationToken cancellationToken);

Task<Announcement?> ClaimNextAsync(
    DateTime now,
    CancellationToken cancellationToken);

Task MarkCompletedAsync(
    long announcementId,
    DateTime updatedAt,
    CancellationToken cancellationToken);
